apiVersion: v1
kind: Service

metadata:
  name: checkout

spec:
  selector:
    app: checkout

  ports:
    - port: 80
      targetPort: {{ .Values.service.port }}
      protocol: TCP

  type: {{ .Values.service.type }}
