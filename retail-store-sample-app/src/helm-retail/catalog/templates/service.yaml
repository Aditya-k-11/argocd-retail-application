apiVersion: v1
kind: Service

metadata:
  name: catalog

spec:
  selector:
    app: catalog

  ports:
    - port: 80
      targetPort: {{ .Values.service.port }}
      protocol: TCP

  type: {{ .Values.service.type }}
